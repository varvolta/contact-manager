// import { useContacts } from '../hooks/useContacts';

export function ContactsPage() {
    return (
        <div className="p-6">
            <h1 className="text-2xl font-bold mb-4">Select a contact</h1>
            <p className="text-gray-600">Choose a contact from the sidebar to view details</p>
        </div>
    );
}